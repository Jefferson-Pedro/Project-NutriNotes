import { AlternativeMainColorsDirective } from './alternative-main-colors.directive';

describe('alternativeMainColorsDirective', () => {
  it('should create an instance', () => {
    const directive = new AlternativeMainColorsDirective();
    expect(directive).toBeTruthy();
  });
});
