import { CssDirective } from './css.directive';

describe('CssDirective', () => {
  it('should create an instance', () => {
    const directive = new CssDirective();
    expect(directive).toBeTruthy();
  });
});
